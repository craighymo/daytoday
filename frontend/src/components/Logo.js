import LogoIcon from "./../icons/LogoIcon";

// LogoIcon and Logo used in navbar, login and registration

// used spaces for text because letter-spacing in CSS just wasn't looking as good

function Logo() {
  return (
    <div>
      <LogoIcon /> d a y <b>t o d a y</b>
    </div>
  );
}

export default Logo;
